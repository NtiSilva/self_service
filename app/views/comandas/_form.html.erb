<%= form_with(model: comanda, local: true) do |form| %>
  <% if comanda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comanda.errors.count, "error") %> prohibited this comanda from being saved:</h2>

      <ul>
      <% comanda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome_cliente %>
    <%= form.text_field :nome_cliente %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
